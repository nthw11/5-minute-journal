import type { Meta, StoryObj } from '@storybook/react'

import PrintViewLayout from './PrintViewLayout'

const meta: Meta<typeof PrintViewLayout> = {
  component: PrintViewLayout,
}

export default meta

type Story = StoryObj<typeof PrintViewLayout>

export const Primary: Story = {}
